// Import Stockfish engine from CDN
// stockfish.js natively runs as a web worker and sets up its own onmessage/postMessage
importScripts("https://cdnjs.cloudflare.com/ajax/libs/stockfish.js/10.0.2/stockfish.js");
